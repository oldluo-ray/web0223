/**
    1. 正则表达式如何创建: 
        1.1  正则字面量 /a/参数
        1.2  new RegExp('正则规则','正则参数')


        参数: i 忽略  g 全局


    2. 特殊字符: 
       元素字符:  \d, \s, \w  ... 
       限定符: *  ?  + {n} ..
       其他: [] () \ ... 

    3. 正则的使用场景(操作字符串): 
      3.1 匹配 正则.test(字符串)
      3.2 提取 字符串.match(正则)
      3.3 替换 字符串.replace(正则, 替换的字符)

 */
